© 2026 AO Kaspersky Lab
This script is licensed under the MIT License

---
 CMakeLists.txt                                | 20 +++++++++-------
 cmake/abseil-cpp.cmake                        | 14 ++++++++---
 cmake/cares.cmake                             | 12 ++++++++--
 cmake/protobuf.cmake                          | 15 +++++++++---
 cmake/ssl.cmake                               | 15 +++++++++---
 cmake/zlib.cmake                              |  3 +++
 include/grpc/event_engine/port.h              |  5 +++-
 include/grpc/support/port_platform.h          | 24 +++++++++++++++++++
 src/core/lib/event_engine/tcp_socket_utils.cc |  2 +-
 src/core/lib/iomgr/ev_poll_posix.cc           | 14 ++++++++++-
 src/core/lib/iomgr/port.h                     |  8 +++++++
 .../lib/iomgr/socket_utils_common_posix.cc    |  2 ++
 src/core/util/posix/directory_reader.cc       |  2 +-
 src/core/util/subprocess_posix.cc             | 20 ++++++++++++++++
 14 files changed, 133 insertions(+), 23 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 391d113ea..6c20fd4a5 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -75,26 +75,29 @@ set(gRPC_ZLIB_PROVIDER "module" CACHE STRING "Provider of zlib library")
 set_property(CACHE gRPC_ZLIB_PROVIDER PROPERTY STRINGS "module" "package")
 
 set(gRPC_CARES_PROVIDER "module" CACHE STRING "Provider of c-ares library")
-set_property(CACHE gRPC_CARES_PROVIDER PROPERTY STRINGS "module" "package")
+set_property(CACHE gRPC_CARES_PROVIDER PROPERTY STRINGS "module" "package" "download")
 
 set(gRPC_RE2_PROVIDER "module" CACHE STRING "Provider of re2 library")
 set_property(CACHE gRPC_RE2_PROVIDER PROPERTY STRINGS "module" "package")
 
 set(gRPC_SSL_PROVIDER "module" CACHE STRING "Provider of ssl library")
-set_property(CACHE gRPC_SSL_PROVIDER PROPERTY STRINGS "module" "package")
+set_property(CACHE gRPC_SSL_PROVIDER PROPERTY STRINGS "module" "package" "download")
 
 set(gRPC_PROTOBUF_PROVIDER "module" CACHE STRING "Provider of protobuf library")
-set_property(CACHE gRPC_PROTOBUF_PROVIDER PROPERTY STRINGS "module" "package")
+set_property(CACHE gRPC_PROTOBUF_PROVIDER PROPERTY STRINGS "module" "package" "download")
 
 if(gRPC_BUILD_TESTS)
   set(gRPC_BENCHMARK_PROVIDER "module" CACHE STRING "Provider of benchmark library")
   set_property(CACHE gRPC_BENCHMARK_PROVIDER PROPERTY STRINGS "module" "package")
+  if(KOS)
+    add_compile_definitions(GRPC_PORT_ISOLATED_RUNTIME=1)
+  endif()
 else()
   set(gRPC_BENCHMARK_PROVIDER "none")
 endif()
 
 set(gRPC_ABSL_PROVIDER "module" CACHE STRING "Provider of absl library")
-set_property(CACHE gRPC_ABSL_PROVIDER PROPERTY STRINGS "module" "package")
+set_property(CACHE gRPC_ABSL_PROVIDER PROPERTY STRINGS "module" "package" "download")
 
 set(gRPC_ABSL_USED_TARGETS
   absl_absl_check
@@ -265,7 +268,7 @@ set(gRPC_OPENTELEMETRY_PROVIDER "package")
 
 set(gRPC_USE_PROTO_LITE OFF CACHE BOOL "Use the protobuf-lite library")
 
-if(UNIX)
+if(UNIX OR KOS)
   if(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
     set(_gRPC_PLATFORM_LINUX ON)
     if(NOT CMAKE_CROSSCOMPILING AND CMAKE_SIZEOF_VOID_P EQUAL 4)
@@ -326,6 +329,7 @@ endif()
 if (MINGW)
   add_definitions(-D_WIN32_WINNT=0x600)
 endif()
+set(_gRPC_C_CXX_FLAGS "${_gRPC_C_CXX_FLAGS} -Wno-attributes -Wno-return-type")
 set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${_gRPC_C_CXX_FLAGS}")
 set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${_gRPC_C_CXX_FLAGS}")
 
@@ -342,12 +346,12 @@ else()
   set(_gRPC_CORE_NOSTDCXX_FLAGS "")
 endif()
 
-if(UNIX AND NOT HAIKU)
+if((UNIX OR KOS) AND NOT HAIKU)
   # -pthread does more than -lpthread
   set(THREADS_PREFER_PTHREAD_FLAG ON)
   find_package(Threads)
   set(_gRPC_ALLTARGETS_LIBRARIES ${CMAKE_DL_LIBS} m Threads::Threads)
-  if(_gRPC_PLATFORM_LINUX OR _gRPC_PLATFORM_POSIX)
+  if((_gRPC_PLATFORM_LINUX OR _gRPC_PLATFORM_POSIX) AND NOT KOS)
     find_library(LIBRT rt)
     if(LIBRT)
       set(_gRPC_ALLTARGETS_LIBRARIES ${_gRPC_ALLTARGETS_LIBRARIES} rt)
@@ -357,7 +361,7 @@ endif()
 
 include(CheckCXXSourceCompiles)
 
-if(UNIX OR APPLE)
+if(UNIX OR APPLE OR KOS)
   # Some systems require the __STDC_FORMAT_MACROS macro to be defined
   # to get the fixed-width integer type formatter macros.
   check_cxx_source_compiles("#include <inttypes.h>
diff --git a/cmake/abseil-cpp.cmake b/cmake/abseil-cpp.cmake
index bacaad75f..b6cfa5751 100644
--- a/cmake/abseil-cpp.cmake
+++ b/cmake/abseil-cpp.cmake
@@ -15,18 +15,26 @@
 if(TARGET absl::strings)
   # If absl is included already, skip including it.
   # (https://github.com/grpc/grpc/issues/29608)
-elseif(gRPC_ABSL_PROVIDER STREQUAL "module")
+elseif(gRPC_ABSL_PROVIDER STREQUAL "module" OR gRPC_ABSL_PROVIDER STREQUAL "download")
   if(NOT ABSL_ROOT_DIR)
     set(ABSL_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR}/third_party/abseil-cpp)
   endif()
-  if(EXISTS "${ABSL_ROOT_DIR}/CMakeLists.txt")
+  if(EXISTS "${ABSL_ROOT_DIR}/CMakeLists.txt" OR gRPC_ABSL_PROVIDER STREQUAL "download")
     if(gRPC_INSTALL)
       # When gRPC_INSTALL is enabled and Abseil will be built as a module,
       # Abseil will be installed along with gRPC for convenience.
       set(ABSL_ENABLE_INSTALL ON)
     endif()
     set(ABSL_PROPAGATE_CXX_STD ON)
-    add_subdirectory(${ABSL_ROOT_DIR} third_party/abseil-cpp)
+    if (gRPC_ABSL_PROVIDER STREQUAL "module")
+      add_subdirectory(${ABSL_ROOT_DIR} third_party/abseil-cpp)
+    else()
+      message(STATUS "Downloading abseil-cpp-kos")
+      FetchContent_Declare(abseil-cpp-kos
+                           GIT_REPOSITORY https://github.com/KasperskyLab/abseil-cpp-kos.git
+                           GIT_TAG        kosce_v1.4.0)
+      FetchContent_MakeAvailable (abseil-cpp-kos)
+    endif()
   else()
     message(WARNING "gRPC_ABSL_PROVIDER is \"module\" but ABSL_ROOT_DIR is wrong")
   endif()
diff --git a/cmake/cares.cmake b/cmake/cares.cmake
index b3209ac36..922309ba8 100644
--- a/cmake/cares.cmake
+++ b/cmake/cares.cmake
@@ -12,7 +12,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
-if(gRPC_CARES_PROVIDER STREQUAL "module")
+if(gRPC_CARES_PROVIDER STREQUAL "module" OR gRPC_CARES_PROVIDER STREQUAL "download")
   if(NOT CARES_ROOT_DIR)
     set(CARES_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR}/third_party/cares/cares)
   endif()
@@ -22,7 +22,15 @@ if(gRPC_CARES_PROVIDER STREQUAL "module")
     # See https://github.com/grpc/grpc/issues/17255
     set(HAVE_LIBNSL OFF CACHE BOOL "avoid cares dependency on libnsl")
   endif()
-  add_subdirectory("${CARES_ROOT_DIR}" third_party/cares/cares)
+  if(gRPC_CARES_PROVIDER STREQUAL "module")
+    add_subdirectory("${CARES_ROOT_DIR}" third_party/cares/cares)
+  else()
+    message(STATUS "Downloading c-ares-kos")
+    FetchContent_Declare(c-ares-kos
+                         GIT_REPOSITORY https://github.com/KasperskyLab/c-ares-kos.git
+                         GIT_TAG        kosce_v1.4.0)
+    FetchContent_MakeAvailable (c-ares-kos)
+  endif()
 
   if(TARGET c-ares)
     set(_gRPC_CARES_LIBRARIES c-ares)
diff --git a/cmake/protobuf.cmake b/cmake/protobuf.cmake
index ad80ca6e4..484b9f6df 100644
--- a/cmake/protobuf.cmake
+++ b/cmake/protobuf.cmake
@@ -12,7 +12,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
-if(gRPC_PROTOBUF_PROVIDER STREQUAL "module")
+if(gRPC_PROTOBUF_PROVIDER STREQUAL "module" OR gRPC_PROTOBUF_PROVIDER STREQUAL "download")
   # Building the protobuf tests require gmock what is not part of a standard protobuf checkout.
   # Disable them unless they are explicitly requested from the cmake command line (when we assume
   # gmock is downloaded to the right location inside protobuf).
@@ -28,9 +28,18 @@ if(gRPC_PROTOBUF_PROVIDER STREQUAL "module")
     set(PROTOBUF_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR}/third_party/protobuf)
   endif()
 
-  if(EXISTS "${PROTOBUF_ROOT_DIR}/CMakeLists.txt")
+  if(EXISTS "${PROTOBUF_ROOT_DIR}/CMakeLists.txt" OR gRPC_PROTOBUF_PROVIDER STREQUAL "download")
     set(protobuf_MSVC_STATIC_RUNTIME OFF CACHE BOOL "Link static runtime libraries")
-    add_subdirectory(${PROTOBUF_ROOT_DIR} third_party/protobuf)
+    if(gRPC_PROTOBUF_PROVIDER STREQUAL "module")
+      add_subdirectory(${PROTOBUF_ROOT_DIR} third_party/protobuf)
+    else()
+      message(STATUS "Downloading protobuf-kos")
+      FetchContent_Declare(protobuf-kos
+                           GIT_REPOSITORY https://github.com/KasperskyLab/protobuf-kos.git
+                           GIT_TAG        kosce_v1.4.0)
+      FetchContent_MakeAvailable (protobuf-kos)
+      set(PROTOBUF_ROOT_DIR ${protobuf_SOURCE_DIR})
+    endif()
     if(TARGET ${_gRPC_PROTOBUF_LIBRARY_NAME})
       set(_gRPC_PROTOBUF_LIBRARIES ${_gRPC_PROTOBUF_LIBRARY_NAME})
     endif()
diff --git a/cmake/ssl.cmake b/cmake/ssl.cmake
index bf43e4758..5a3cdce60 100644
--- a/cmake/ssl.cmake
+++ b/cmake/ssl.cmake
@@ -16,12 +16,12 @@
 # transitively so `_gRPC_SSL_INCLUDE_DIR` should be set for gRPC
 # to find header files.
 
-if(gRPC_SSL_PROVIDER STREQUAL "module")
+if(gRPC_SSL_PROVIDER STREQUAL "module" OR gRPC_SSL_PROVIDER STREQUAL "download")
   if(NOT BORINGSSL_ROOT_DIR)
     set(BORINGSSL_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR}/third_party/boringssl-with-bazel)
   endif()
 
-  if(EXISTS "${BORINGSSL_ROOT_DIR}/CMakeLists.txt")
+  if(EXISTS "${BORINGSSL_ROOT_DIR}/CMakeLists.txt" OR gRPC_SSL_PROVIDER STREQUAL "download")
     if(CMAKE_GENERATOR MATCHES "Visual Studio")
       if(CMAKE_VERSION VERSION_LESS 3.13)
         # Visual Studio build with assembly optimizations is broken for older
@@ -40,7 +40,16 @@ if(gRPC_SSL_PROVIDER STREQUAL "module")
       endif()
     endif()
 
-    add_subdirectory(${BORINGSSL_ROOT_DIR} third_party/boringssl-with-bazel)
+    if(gRPC_SSL_PROVIDER STREQUAL "module")
+      add_subdirectory(${BORINGSSL_ROOT_DIR} third_party/boringssl-with-bazel)
+    else()
+      message(STATUS "Downloading BoringSSL-kos")
+      FetchContent_Declare(BoringSSL-kos
+                           GIT_REPOSITORY https://github.com/KasperskyLab/boringssl-kos.git
+                           GIT_TAG        kosce_v1.4.0)
+      FetchContent_MakeAvailable (BoringSSL-kos)
+      set(BORINGSSL_ROOT_DIR ${BoringSSL_SOURCE_DIR})
+    endif()
     if(TARGET ssl)
       set(_gRPC_SSL_LIBRARIES ssl crypto)
       set(_gRPC_SSL_INCLUDE_DIR ${BORINGSSL_ROOT_DIR}/src/include)
diff --git a/cmake/zlib.cmake b/cmake/zlib.cmake
index 9d658921d..1fd398702 100644
--- a/cmake/zlib.cmake
+++ b/cmake/zlib.cmake
@@ -49,6 +49,9 @@ elseif(gRPC_ZLIB_PROVIDER STREQUAL "package")
   # package ("MODULE"), while on Windows the user is likely to have installed
   # zlib using cmake ("CONFIG").
   # See https://cmake.org/cmake/help/v3.6/module/FindZLIB.html
+  if(KOS)
+    set(ZLIB_USE_STATIC_LIBS YES)
+  endif()
   find_package(ZLIB REQUIRED)
 
   if(TARGET ZLIB::ZLIB)
diff --git a/include/grpc/event_engine/port.h b/include/grpc/event_engine/port.h
index 3055a667e..4a9f0865e 100644
--- a/include/grpc/event_engine/port.h
+++ b/include/grpc/event_engine/port.h
@@ -20,11 +20,14 @@
 #if defined(GPR_ANDROID) || defined(GPR_LINUX) || defined(GPR_APPLE) ||     \
     defined(GPR_FREEBSD) || defined(GPR_OPENBSD) || defined(GPR_SOLARIS) || \
     defined(GPR_AIX) || defined(GPR_NACL) || defined(GPR_FUCHSIA) ||        \
-    defined(GRPC_POSIX_SOCKET) || defined(GPR_NETBSD)
+    defined(GRPC_POSIX_SOCKET) || defined(GPR_NETBSD) || defined(GPR_KOS)
 #define GRPC_EVENT_ENGINE_POSIX
 #include <arpa/inet.h>
 #include <netdb.h>
 #include <netinet/in.h>
+#if defined(GPR_KOS)
+#include <sys/uio.h>
+#endif
 #include <sys/socket.h>
 #include <unistd.h>
 #elif defined(GPR_WINDOWS)
diff --git a/include/grpc/support/port_platform.h b/include/grpc/support/port_platform.h
index 04a79ef3c..9db54cc20 100644
--- a/include/grpc/support/port_platform.h
+++ b/include/grpc/support/port_platform.h
@@ -424,6 +424,30 @@
 #else /* _LP64 */
 #define GPR_ARCH_32 1
 #endif /* _LP64 */
+#elif defined(__KOS__)
+#define GPR_PLATFORM_STRING "KasperskyOS"
+#ifndef _BSD_SOURCE
+#define _BSD_SOURCE
+#endif
+#define GPR_KOS 1
+#define GPR_CPU_POSIX 1
+#define GPR_GCC_ATOMIC 1
+#define GPR_GCC_TLS 1
+#define GPR_POSIX_LOG 1
+#define GPR_POSIX_ENV 1
+#define GPR_POSIX_TMPFILE 1
+#define GPR_POSIX_STAT 1
+#define GPR_POSIX_STRING 1
+#undef GPR_POSIX_SUBPROCESS
+#define GPR_POSIX_SYNC 1
+#define GPR_POSIX_TIME 1
+#define GPR_GETPID_IN_UNISTD_H 1
+#define GPR_SUPPORT_CHANNELS_FROM_FD 1
+#ifdef _LP64
+#define GPR_ARCH_64 1
+#else /* _LP64 */
+#define GPR_ARCH_32 1
+#endif /* _LP64 */
 #elif defined(__native_client__)
 #define GPR_PLATFORM_STRING "nacl"
 #ifndef _BSD_SOURCE
diff --git a/src/core/lib/event_engine/tcp_socket_utils.cc b/src/core/lib/event_engine/tcp_socket_utils.cc
index 2ba754595..e796ead22 100644
--- a/src/core/lib/event_engine/tcp_socket_utils.cc
+++ b/src/core/lib/event_engine/tcp_socket_utils.cc
@@ -97,7 +97,7 @@ absl::StatusOr<std::string> ResolvedAddrToUnixPathIfPossible(
         absl::StrCat("Socket family is not AF_UNIX: ", addr->sa_family));
   }
   const sockaddr_un* unix_addr = reinterpret_cast<const sockaddr_un*>(addr);
-#ifdef GPR_APPLE
+#if defined(GPR_APPLE) || defined(__KOS__)
   int len = resolved_addr->size() - sizeof(unix_addr->sun_family) -
             sizeof(unix_addr->sun_len) - 1;
 #else
diff --git a/src/core/lib/iomgr/ev_poll_posix.cc b/src/core/lib/iomgr/ev_poll_posix.cc
index 32d42c36e..973d54c5d 100644
--- a/src/core/lib/iomgr/ev_poll_posix.cc
+++ b/src/core/lib/iomgr/ev_poll_posix.cc
@@ -1027,7 +1027,13 @@ static grpc_error_handle pollset_work(grpc_pollset* pollset,
       GRPC_TRACE_LOG(polling, INFO) << pollset << " poll=" << r;
 
       if (r < 0) {
+#ifdef __KOS__
+        // KasperskyOS returns errors for invalid file descriptors. We ignore these errors,
+        // expecting the invalid descriptors to be removed in the next loop iteration.
+        if (errno != EINTR && errno != EBADF && errno != EINVAL && errno != EACCES) {
+#else
         if (errno != EINTR) {
+#endif
           work_combine_error(&error, GRPC_OS_ERROR(errno, "poll"));
         }
 
@@ -1149,7 +1155,13 @@ static void pollset_shutdown(grpc_pollset* pollset, grpc_closure* closure) {
 }
 
 static int poll_deadline_to_millis_timeout(grpc_core::Timestamp deadline) {
-  if (deadline == grpc_core::Timestamp::InfFuture()) return -1;
+  if (deadline == grpc_core::Timestamp::InfFuture())
+#ifndef __KOS__
+    return -1;
+#else
+    // KasperskyOS doesn't support infinite poll wait.
+    return INT_MAX;
+#endif
   if (deadline.is_process_epoch()) return 0;
   int64_t n = (deadline - grpc_core::Timestamp::Now()).millis();
   if (n < 0) return 0;
diff --git a/src/core/lib/iomgr/port.h b/src/core/lib/iomgr/port.h
index 4899e1734..ab10954bb 100644
--- a/src/core/lib/iomgr/port.h
+++ b/src/core/lib/iomgr/port.h
@@ -180,6 +180,14 @@
 #define GRPC_POSIX_SOCKET 1
 #define GRPC_POSIX_SOCKETUTILS 1
 #define GRPC_POSIX_WAKEUP_FD 1
+#elif defined(GPR_KOS)
+#define GRPC_HAVE_IFADDRS 1
+#define GRPC_HAVE_UNIX_SOCKET 1
+#undef GRPC_POSIX_FORK
+#define GRPC_POSIX_NO_SPECIAL_WAKEUP_FD 1
+#define GRPC_POSIX_SOCKET 1
+#define GRPC_POSIX_SOCKETUTILS 1
+#define GRPC_POSIX_WAKEUP_FD 1
 #elif defined(GPR_NACL)
 #define GRPC_HAVE_ARPA_NAMESER 1
 #define GRPC_POSIX_NO_SPECIAL_WAKEUP_FD 1
diff --git a/src/core/lib/iomgr/socket_utils_common_posix.cc b/src/core/lib/iomgr/socket_utils_common_posix.cc
index 8b1af9db7..1e7c22ff0 100644
--- a/src/core/lib/iomgr/socket_utils_common_posix.cc
+++ b/src/core/lib/iomgr/socket_utils_common_posix.cc
@@ -229,7 +229,9 @@ void probe_so_reuseport_once(void) {
 }
 
 bool grpc_is_socket_reuse_port_supported() {
+#ifndef __KOS__
   gpr_once_init(&g_probe_so_reuesport_once, probe_so_reuseport_once);
+#endif
   return g_support_so_reuseport;
 }
 
diff --git a/src/core/util/posix/directory_reader.cc b/src/core/util/posix/directory_reader.cc
index 00f5a6a45..1c1304aa4 100644
--- a/src/core/util/posix/directory_reader.cc
+++ b/src/core/util/posix/directory_reader.cc
@@ -25,7 +25,7 @@
 #include "absl/strings/string_view.h"
 
 #if defined(GPR_LINUX) || defined(GPR_ANDROID) || defined(GPR_FREEBSD) || \
-    defined(GPR_APPLE) || defined(GPR_NETBSD)
+    defined(GPR_APPLE) || defined(GPR_NETBSD) || defined(GPR_KOS)
 
 #include <dirent.h>
 
diff --git a/src/core/util/subprocess_posix.cc b/src/core/util/subprocess_posix.cc
index ef4512b37..6ab1c10be 100644
--- a/src/core/util/subprocess_posix.cc
+++ b/src/core/util/subprocess_posix.cc
@@ -248,4 +248,24 @@ void gpr_subprocess_interrupt(gpr_subprocess* p) {
 
 int gpr_subprocess_get_process_id(gpr_subprocess* p) { return p->pid; }
 
+#elif defined(__KOS__)
+
+#include <grpc/support/log.h>
+#include "src/core/util/subprocess.h"
+
+#define NOT_SUPPORTED_ON_KOS(FN)                               \
+FN                                                             \
+{                                                              \
+  grpc_absl_log(GPR_ERROR, #FN ": not supported in KasperskyOS");  \
+  abort();                                                     \
+}
+
+NOT_SUPPORTED_ON_KOS(const char* gpr_subprocess_binary_extension())
+NOT_SUPPORTED_ON_KOS(gpr_subprocess* gpr_subprocess_create(int, const char**))
+NOT_SUPPORTED_ON_KOS(gpr_subprocess* gpr_subprocess_create_with_envp(int, const char**, int, const char**))
+NOT_SUPPORTED_ON_KOS(bool gpr_subprocess_communicate(gpr_subprocess*, std::string&, std::string*, std::string*))
+NOT_SUPPORTED_ON_KOS(void gpr_subprocess_destroy(gpr_subprocess*))
+NOT_SUPPORTED_ON_KOS(int gpr_subprocess_join(gpr_subprocess*))
+NOT_SUPPORTED_ON_KOS(void gpr_subprocess_interrupt(gpr_subprocess* p))
+NOT_SUPPORTED_ON_KOS(int gpr_subprocess_get_process_id(gpr_subprocess*))
 #endif  // GPR_POSIX_SUBPROCESS
-- 
2.51.1

